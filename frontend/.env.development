# Always use the local Firebase emulator suite for `npm run dev`, even though real project
# keys exist in .env.local - this is what makes it safe to have both files present at once.
VITE_USE_FIREBASE_EMULATOR=true
